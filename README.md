# Quick Startup
1. clone the repo

2. create virtual env and install dependencies
```
cd Event-Ticket-System
python -m venv venv
pip install flask flask-cors flasksqlalchemy
```

3. start app
```
cd backend
python app.py
```
